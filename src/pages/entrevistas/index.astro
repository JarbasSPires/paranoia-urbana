---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const base = import.meta.env.BASE_URL;
const items = (await getCollection('interviews')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---
<BaseLayout title="Entrevistas">
  <h2 data-i18n="interviews.heading">Entrevistas</h2>
  <ul class="article-list">
    {items.map((item) => (
      <li>
        <p class="kicker">{item.data.band ?? 'Entrevista'}</p>
        <h3><a href={`${base}entrevistas/${item.slug}/`}>
          <span data-lang-content="pt">{item.data.title}</span>
          <span data-lang-content="en">{item.data.titleEn ?? item.data.title}</span>
        </a></h3>
        <p>
          <span data-lang-content="pt">{item.data.summary}</span>
          <span data-lang-content="en">{item.data.summaryEn ?? item.data.summary}</span>
        </p>
      </li>
    ))}
  </ul>
</BaseLayout>
